{
  "schema_version": 1,
  "generated": "2026-05-06T00:00:00Z",
  "issues": [
    {
      "id": "DEAD-001",
      "reader": "dead_code",
      "file": "validate.py",
      "line": 76,
      "severity": "warning",
      "description": "The `\"tp\"` key initialised in the `buckets` defaultdict lambda is never written to and never read; per-confidence TP counts are recomputed from scratch on lines 89–92 using a separate list comprehension.",
      "proposed_fix": "Change the defaultdict lambda on line 76 from `{\"tp\": 0, \"total\": 0}` to `{\"total\": 0}` and remove the unused `\"tp\"` key.",
      "status": "fixed",
      "skip_reason": null
    },
    {
      "id": "LOGIC-001",
      "reader": "logic",
      "file": "pipeline/output.py",
      "line": 123,
      "severity": "warning",
      "description": "The guard `if not rows` checks whether any papers exist for the run, but it should check whether any *relevant* papers exist; when all classified papers are NOT_RELEVANT the digest body is empty yet the fallback message is never written.",
      "proposed_fix": "Change `if not rows:` to `if not relevant:`",
      "status": "fixed",
      "skip_reason": null
    },
    {
      "id": "LOGIC-002",
      "reader": "logic",
      "file": "validate.py",
      "line": 139,
      "severity": "error",
      "description": "The `base_dir` computed on line 135 from the ground-truth file's directory is never used; `run_validation` is called with the script's own directory instead, so PDF paths are resolved relative to the wrong directory whenever the ground-truth file is not co-located with `validate.py`.",
      "proposed_fix": "Change `run_validation(papers, os.path.dirname(os.path.abspath(__file__)))` to `run_validation(papers, base_dir)`",
      "status": "fixed",
      "skip_reason": null
    }
  ]
}
